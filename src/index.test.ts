describe('example test', () => {
    test('unity', () => {
        // Arrange
        const value = 1;

        // Act
        const actual = value === 1

        // Assert
        expect(actual).toBeTruthy()
    })
})